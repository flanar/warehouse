all:
  children:
    production:
      hosts: localhost
      vars:
        ansible_network_os: cisco.ios.ios
        ansible_python_interpreter: /usr/bin/python3

  vars:
    asnible_connection: local
    root_dir: /home/{{ ansible_user }}/warehouse